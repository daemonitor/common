export class Renderable {
    async render(): Promise<any> {
        // return h(
        //     "div",
        //     Array.from({length: 20}).map(() => {
        //         return h("p", "hi")
        //     })
        // )
        return "..."
    }
}
